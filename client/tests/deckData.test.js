// import DeckData from "../src/deck/DeckData";

// let deckdata = new DeckData();
// let deckdataClone = deckdata.clone;
// const {length} = deckdata._cards;

describe("deckDataTest", function() {


  // it("should check if deck has enough cards at least for 1 round", function() {
  //   expect(deckdata._cards.length).toBeGreaterThan(1);
  // });

  it('should start the game', () => {
    expect(true).toEqual(true)
  })

  // it("should check if topCard returns last card", function() {
  //   let topCard = deckdata.topCard;
  //
  //   expect(topCard).toBe(deckdata._cards[length - 1]);
  // });
  //
  // it("should check if clone clones", function() {
  //   expect(deckdataClone).toEqual(deckdata);
  // });
  //
  // it("should check if deck is empty", function() {
  //   let value = (length === 0);
  //
  //   expect(deckdata.isEmpty).toEqual(value);
  // });
  //
  // it("should check if pop removes 1 card", function() {
  //   deckdataClone.pop();
  //
  //   expect(deckdataClone._cards.length).toBe(length - 1);
  // });

});